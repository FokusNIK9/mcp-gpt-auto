# GitHub Task Queue

Folder structure for remote task execution.

- `inbox/` - Place new tasks here.
- `running/` - Currently active task (on the local machine).
- `done/` - Completed task results.
- `failed/` - Failed task results.
- `reports/` - Human-readable reports for each task.

Do not delete `.gitkeep` files.
