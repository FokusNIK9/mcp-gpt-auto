{
  "name": "mcp-gpt-auto",
  "version": "0.1.0",
  "description": "Local MCP gateway for controlled agentic work on a developer PC.",
  "type": "module",
  "private": true,
  "bin": {
    "mcp-gpt-auto": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "runner:once": "node dist/runner/github-task-runner.js --once",
    "runner:loop": "node dist/runner/github-task-runner.js --loop",
    "test:redaction": "node scripts/test-redaction.js",
    "action:bridge": "node dist/action-bridge/server.js",
    "action:openapi": "node dist/action-bridge/openapi.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@types/express": "^5.0.6",
    "express": "^5.2.1",
    "ws": "^8.20.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/ws": "^8.18.1",
    "tsx": "^4.7.1",
    "typescript": "^5.4.0"
  }
}
